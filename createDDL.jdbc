CREATE TABLE USERMONEY (ID INTEGER NOT NULL, BALANCE DOUBLE NOT NULL, RFID VARCHAR, USERNAME VARCHAR NOT NULL, PRIMARY KEY (ID))
CREATE TABLE TRANSACTION (ID INTEGER IDENTITY NOT NULL, AMOUNT DOUBLE, CREATED TIMESTAMP NOT NULL, userMoneyRef INTEGER, orderingRef INTEGER, PRIMARY KEY (ID))
CREATE TABLE ORDERING (ID INTEGER IDENTITY NOT NULL, CREATED TIMESTAMP NOT NULL, PRIMARY KEY (ID))
CREATE TABLE ORDERINGPOSITION (ID INTEGER IDENTITY NOT NULL, articleRef INTEGER, orderingRef INTEGER, PRIMARY KEY (ID))
CREATE TABLE ARTICLE (ID INTEGER IDENTITY NOT NULL, AVAILABLE BOOLEAN NOT NULL, NAME VARCHAR NOT NULL, PRICE DOUBLE NOT NULL, PRIMARY KEY (ID))
ALTER TABLE TRANSACTION ADD CONSTRAINT FK_TRANSACTION_userMoneyRef FOREIGN KEY (userMoneyRef) REFERENCES USERMONEY (ID)
ALTER TABLE TRANSACTION ADD CONSTRAINT FK_TRANSACTION_orderingRef FOREIGN KEY (orderingRef) REFERENCES ORDERING (ID)
ALTER TABLE ORDERINGPOSITION ADD CONSTRAINT FK_ORDERINGPOSITION_orderingRef FOREIGN KEY (orderingRef) REFERENCES ORDERING (ID)
ALTER TABLE ORDERINGPOSITION ADD CONSTRAINT FK_ORDERINGPOSITION_articleRef FOREIGN KEY (articleRef) REFERENCES ARTICLE (ID)
